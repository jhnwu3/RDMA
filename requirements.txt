# Core numerics / data
numpy>=1.26
pandas>=2.0
scipy==1.15.2
joblib==1.4.2
tqdm==4.67.1

# Deep learning
torch>=2.0
transformers
tokenizers
accelerate
peft
bitsandbytes
huggingface_hub

# Embeddings / retrieval
faiss_cpu==1.10.0
fastembed==0.6.1
sentence_transformers==3.4.1
sent2vec==0.3.0

# NLP
spacy==3.8.5
scispacy
negspacy
stanza==1.10.1
nltk==3.9.1
fuzzywuzzy==0.18.0
rapidfuzz==3.13.0

# Ontologies
pyhpo

# LLM backends
openai>=1.0.0
groq==0.24.0
langchain_openai
python-dotenv>=1.0.0
ratelimit==2.2.1

# Benchmark framework
pyhealth

# Hyperparameter search — used by the BERT baseline trainers
# (baselines/raredis/{biobert_mrc,bioclinicalbert_ner}_trainer.py)
optuna

# Optional: only needed for the llama.cpp LLM backend
# (rdma/utils/llm_client.py LlamaCppLLMClient, and the GGUF baselines).
# Install separately, since the build is platform-specific:
#   pip install llama-cpp-python
# llama-cpp-python

# NOTE: some baselines import from external repos that are cloned alongside
# RDMA rather than installed from PyPI — FastHPOCR (HPOAnnotator), PhenoGPT
# (run_phenogpt) and BioBERT-MRC. See CITATIONS.md for where to get each.
